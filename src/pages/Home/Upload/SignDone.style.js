import styled from "styled-components";

export const SignDoneStyle = styled.div`
    display: grid;
    gap: 8rem;
    position: relative;
    width: 80%;
    height: 100%;
    margin: auto;
    background: var(--clr-gray-40);
    padding: 4rem;
    overflow: auto;
`;
